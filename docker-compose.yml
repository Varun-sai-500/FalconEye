services:
  api:
    image: falconeye:latest
    build: .
    command: >
      uvicorn api.main:app
      --host 0.0.0.0
      --port 8080
    ports:
      - "8080:8080"
    restart: unless-stopped
    gpus: all